#include <iostream>
#include <stdio.h>

using namespace std;

class InsertionSort {
public:
    static void insertion_sort(int a[], int n);
    static void binary_sort(int a[], int n);
    
private:
    static int count_center(int lf, int rg);
    
    
};